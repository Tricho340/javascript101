"use strict";

var count_substrings = function(haystack, needle) {
  var stringCharacters = haystack.split("");
  stringCharacters.forEach(function() )
};

exports.count_substrings = count_substrings;
